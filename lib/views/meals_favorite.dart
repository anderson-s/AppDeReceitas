import 'package:flutter/material.dart';

class MealsFavorite extends StatelessWidget {
  const MealsFavorite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Refeições Favoritas"),
    );
  }
}
